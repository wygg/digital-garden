---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';

// 数据源：读取 Garden
const posts = await getCollection('garden');

// 排序：按日期倒序
posts.sort((a, b) => {
    const dateA = new Date(a.data.date || 0).valueOf();
    const dateB = new Date(b.data.date || 0).valueOf();
    return dateB - dateA;
});
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title="Digital Garden - Antigravity" description={SITE_DESCRIPTION} />
		<style>
			main {
				width: 960px;
			}
			ul {
				display: flex;
				flex-wrap: wrap;
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			ul li {
				width: calc(50% - 1rem);
			}
			ul li * {
				text-decoration: none;
				transition: 0.2s ease;
			}
			ul li:first-child {
				/* 首篇文章全宽显示 */
				width: 100%;
				margin-bottom: 1rem;
				text-align: center;
			}
			ul li:first-child img {
				width: 100%;
			}
			ul li:first-child .title {
				font-size: 2.369rem;
			}
			ul li img {
				margin-bottom: 0.5rem;
				border-radius: 12px;
			}
			ul li a {
				display: block;
			}
			.title {
				margin: 0;
				color: rgb(var(--black));
				line-height: 1;
			}
			.date {
				margin: 0;
				color: rgb(var(--gray));
			}
			ul li a:hover h4,
			ul li a:hover .date {
				color: rgb(var(--accent));
			}
			ul a:hover img {
				box-shadow: var(--box-shadow);
			}
			@media (max-width: 720px) {
				ul {
					gap: 0.5em;
				}
				ul li {
					width: 100%;
					text-align: center;
				}
				ul li:first-child {
					margin-bottom: 0;
				}
				ul li:first-child .title {
					font-size: 1.563em;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section>
                <div style="margin-bottom: 40px; text-align: center;">
                    <h1 style="font-size: 3em; color: #10b981;">🌿 Digital Garden</h1>
                    <p style="color: #666;">这里生长着我的思想与行动。</p>
                </div>

				<ul>
					{
						posts.map((post) => (
							<li>
								<a href={`/garden/${post.slug}/`}>
									{post.data.heroImage && (
                                        <img width={720} height={360} src={post.data.heroImage} alt="" />
                                    )}
									<h4 class="title">{String(post.data.date || '').substring(0, 10)} {post.data.title}</h4>
									<p class="date">
                                        <time datetime={post.data.date}>
                                            {post.data.date ? new Date(post.data.date).toLocaleDateString('zh-CN') : ''}
                                        </time>
									</p>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
