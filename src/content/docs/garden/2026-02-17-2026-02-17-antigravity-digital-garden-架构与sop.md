---
created: '2026-02-17 23:35:58'
date: '2026-02-19'
title: 2026-02-17 Antigravity Digital Garden 架构与SOP
ts: 1771342558
updated: '2026-02-17 23:35:58'
---

# 🌌 Antigravity Digital Garden 架构与SOP：从博客到数字生命体

**版本**：1.0
**日期**：2026-02-17
**状态**：执行中
**核心目标**：构建一个“赛博格（Cyborg）”式的个人数字生命系统，实现知识的“本地重武器生产”与“移动轻量化展示”的完美分离。

---

## 1. 核心理念升级：The Garden
*   **不仅仅是 Blog**：摒弃“时间线”叙事，转向“知识网”叙事。
*   **双向链接 (Bi-directional Linking)**：像大脑神经元一样，让知识点相互连接。
*   **生长机制**：允许发布“种子”（Seed/Stub），随着思考深入慢慢填充，而非只发布成品。

## 2. 架构设计：Antigravity OS (The Stack)

这是一个不需要 VS Code，由 AI Agent 深度介入的未来派工作流。

*   **🧠 大脑 (Writing & Thinking)**：**Obsidian**
    *   本地最强Markdown编辑器，负责深度思考与笔记录入。
    *   坚守“所见即所得”的思考环境。
*   **🛠️ 工厂 (Coding & Processing)**：**Antigravity + Claude Code**
    *   **Antigravity (Google)**：总架构师与指挥官。负责项目架构、文件搬运、环境配置。
    *   **Claude Code (Anthropic)**：特种兵。负责编写具体脚本、生成代码片段。
*   **🌍 展示 (Presentation)**：**Astro Digital Garden**
    *   **引擎**：Astro (极致轻量 + 静态生成)。
    *   **样式**：Tailwind CSS (响应式 + 极简设计)。
    *   **特性**：原生支持 Obsidian 的 `[[WikiLinks]]`，集成知识图谱 (Knowledge Graph)。
*   **📱 控制 (Mobile Command)**：**Discord + OpenClaw** (未来规划)
    *   移动端控制台，用于远程触发发布、记录灵感。

---

## 3. 物理目录结构 (Physical Architecture)

采用 **“双Buffer”** 机制，确保发布安全。

```text
myAntigravity/ (本地总库)
├── .git/                  # (Root Git) 私有历史，绝对不公开
├── Library/               # 📚 知识库 (Obsidian Vault)
│   ├── Inbox/             # 收集箱
│   ├── 一人大学/          # 项目文档
│   │
│   └── 🟢 Public_Staging/ # 【Buffer A】: 发布暂存区
│       # 这是一个 Obsidian 文件夹。
│       # 作用：作为“准发布”内容的临时停机坪。
│       # 操作：在 Obsidian 里把要发布的笔记拖进来，或打上 #public 标签由脚本抓取。
│
└── 🚀 DigitalGarden/      # 【Buffer B】: 向世界发布的 Astro 项目
    # 这是一个独立的 Astro 项目，也是一个独立的 Git Repo
    # 对应 GitHub: wygg/digital-garden (Public)
    ├── .git/              # (Public Git) 公开历史
    ├── src/
    │   └── content/
    │       └── garden/    # 存放清洗后的 .md 文件 (清洗掉 secret、转换图片链接)
    ├── public/
    │   └── images/        # 存放公开图片
    └── astro.config.mjs   # 站点配置
```

---

## 4. 标准作业程序 (SOP) —— The Workflow

### Phase 1: 挑选 (Select) —— 在 Obsidian 中
1.  在 `Library` 中撰写笔记。
2.  决定发布某篇笔记时，将其移动到 `Library/Public_Staging/`。
3.  **安全检查**：确认笔记内没有包含私钥、密码或不宜公开的私人情感。

### Phase 2: 清洗与预览 (Sync & Review) —— 在 Terminal 中
1.  **呼叫 Antigravity**：执行同步指令（脚本）。
    *   *Antigravity 动作*：扫描 Staging 区 -> 清洗敏感信息 -> 转换 `[[链接]]` 格式 -> 搬运图片 -> 写入 `DigitalGarden`。
2.  **启动预览**：
    ```bash
    cd DigitalGarden
    npm run dev
    ```
3.  **人工审核**：打开浏览器 `http://localhost:4321`。
    *   检查：排版是否错乱？图片是否显示？双链跳转是否正常？
    *   **这是最后一道防线**。

### Phase 3: 发布 (Deploy) —— 确认无误后
1.  **提交代码**：
    ```bash
    cd DigitalGarden
    git add .
    git commit -m "garden update: [简述更新内容]"
    git push origin main
    ```
2.  **等待上线**：GitHub Actions 自动构建，约 1-2 分钟后在全球节点生效。

---

## 5. 初始建设清单 (Checklist)

- [ ] **基础设施**：
    - [x] 创建 `Library/Public_Staging` 目录。
    - [x] 将 `PublicGarden` 重命名并初始化为 `DigitalGarden`。
- [ ] **核心功能开发**：
    - [ ] 配置 `astro.config.mjs` 以支持 `[[WikiLinks]]`。
    - [ ] 开发 `sync_garden.py` (同步脚本)。
    - [ ] 设计极简主题 (Tailwind)。
- [ ] **视觉组件**：
    - [ ] 集成知识图谱 (Graph View)。
    - [ ] 实现反向链接面板 (Backlinks)。